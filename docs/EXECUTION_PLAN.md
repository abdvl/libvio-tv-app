# LIBVIO TV 分步实施计划

日期：2026-09-12。每一步独立提交，记录完成内容、验证命令/结果、尚未覆盖的范围和下一步。已推送 S00 调研提交，现已建立可运行的 0.1.0-dev 原型；以下分别记录实现与验收范围。

| 步骤 | 交付内容 | 完成判断 | 状态 |
| --- | --- | --- | --- |
| S00 实站研究 | 入口、分类、搜索、详情、线路与播放器调查；设计参数；只读探针 | 浏览器证据＋独立 HTML 读取＋离线契约测试 | 完成，见验证记录 |
| S01 播放可行性与工程 | 独立 Android 包/构建，简化测试播放器；一部影片两条不同 provider；原生解析优先，必要时试验 WebView 解析桥 | 在 Android 获得当前媒体 URL 和实际首帧，持续播放、暂停/跳转、切源，并记录请求头要求 | 已通过 Android TV 模拟器 HD7 / HD2 原生首帧、暂停、快进和切换验证 |
| S02 站点与数据适配 | 发布入口、最后成功 host、候选健康检查/故障恢复；首页、目录、详情结构化数据 | 公共页面集＋模拟超时/验证页/域名变化；原生数据层测试通过 | 已实现基础版；验收边界见下文 |
| S03 原生页面与蓝色主题 | Olevod 组件迁移：导航、首页最近播放/推荐、分类小首页、目录与筛选 | 首批真实 App 截图；主要遥控器焦点和返回路径跑通 | 已实现基础版；验收边界见下文 |
| S04 完整播放与本地记录 | 八项控制、普通/全屏、线路浮层、十集分组；本地历史/收藏、影片别名、续播 | 切源保集数、保进度；进程重启；换 host；坏源不覆盖记录 | 已实现基础版；验收边界见下文 |
| S05 搜索和恢复 | 中文输入与三栏搜索、连续追加、旧请求取消；站点设置、空/错误/离线状态 | 无重复页、无焦点丢失、无旧结果覆盖、全部失败后可重试 | 已实现基础版；验收边界见下文 |
| S06 验收与打包 | 单元/集成测试、构建、lint、模拟器截图、Google TV 真机测试；独立签名和版本资产 | 验证记录、APK 与 SHA256SUMS；真机结果与本地结果分列 | 已实现基础版；验收边界见下文 |

## S01 具体拆分

1. 建立 `com.libvio.tv` 的最小原生工程，沿用 Olevod 已验证的构建版本，不移入账号、凭据、签名密钥和更新仓库设置。
2. 用已观察的详情→HD7/HD2 首集路径做数据输入，保留 `routeId` 与 `internalId` 两种身份。
3. 将 `player_aaaa.url` 视为不透明输入，验证每种 `from` 的真实解析方法；解析结果不得写入日志或长期存储。
4. 如试验 WebView 解析桥，完成网页 JS 运行、正确正片识别、获取必要头信息、停止网页媒体并移交 Media3、释放 WebView 的完整过程。失败就明确该 provider 暂不支持，不能以网页能播宣称 TV 已适配。
5. 对两条源分别记录：解析结果类别、首帧、时长、持续播放、暂停恢复、±30 秒、后台恢复；仅保留去敏摘要。
6. 通过后再大规模移植界面，避免先完成所有页面却发现主要线路无法原生播放。

## 后续代码目录建议

```text
app/src/main/java/com/libvio/tv/
  MainActivity.kt
  LibvioApp.kt
  ui/                 # Olevod 组件移植与蓝色主题
  data/host/          # HostRepository、持久化与健康检查
  data/catalog/       # LibvioRepository、HTML 解析、分页与搜索
  data/local/         # 历史、收藏、搜索记录、影片别名
  playback/           # PlaybackSession、Resolver、Media3、可选 WebView 解析桥
app/src/test/         # 纯数据/解析/状态机回归
app/src/androidTest/  # 原生网络/播放器/遥控器 UI 验证
docs/verification/   # 按版本记录证据与边界
```

## S00 当时的完成证据（历史快照）

- 新仓库初始状态包含 README、LICENSE 和 `.gitignore`，尚无应用代码；远程为 `https://github.com/abdvl/libvio-tv-app.git`。
- 对照的 Olevod 工作区本轮没有修改；读取的本地提交是 `0fec7424e1d761373e00c52c0c816e09a42cf4fe`。
- 入口四域名、`.lat` 页面、`.cam` 样本详情、中文搜索、HD7 与 HD2 浏览器视频均有观察记录。
- 只读探针实际读取三条首集配置，独立核对 source/from、sid、nid、encrypt 与内部 ID。
- 11 个离线契约测试通过。测试只验证 Python 调研工具，不代表 Android 适配器或 UI 已完成。
- 尚无 Android 工程、APK、真机验收或远程发布；本次文档不能标为 v0.1 版本已实现。

## 0.1.0-dev 本轮落地

- S01：独立包、工程、原生播放器已建立。HD7 与 HD2 实际出首帧并通过播放中/暂停中切换；HD2 使用 API 30+ 的系统 MediaParser，避免 Media3 1.7.1 在该 HEVC 样本上的配置解析越界。
- S02：入口发现、缓存、健康检测、选站、失败排除当前 host 后重试；同站 HTTPS 有界重定向；首页分区、目录、分页、筛选、搜索、详情、在线/下载线路区分已实现。实站验证 `.lat` / `.cam` 和中文片名；完整超时、入口失效与域名迁移故障注入待补。
- S03：已移植导航、完整海报、最近播放展开卡、前二突出的分类榜、目录浮层、历史双列卡；14 张原生截图已建立。字体 1.3、全遥控器路径、五条最近记录同时展开/滚动待验收。
- S04：普通/全屏、八按钮、十集分组、线路与速度浮层、本地历史与收藏已实现；暂停状态换源通过。进程恢复入口已补齐；强杀进程、旧域名换新 routeId、时长差异、快速连切与坏源注入仍待系统验收。
- S05：三栏搜索、中文系统输入、提交查询、保存搜索记录、切换页面取消旧请求、去重追加、设置与错误重试已实现；首页初始焦点与弹层恢复已有自动化覆盖。中文 IME 真机输入、长列表焦点恢复和离线流程待验收。
- S06：调试 APK、单元测试、lint、模拟器实站测试及截图已建立；正式签名/更新器/Release、API 26–29 播放兼容、Google TV 真机和长播尚未完成。

本轮是可安装开发原型，不标为所有发布验收通过。下一步按 [原型验证记录](verification/prototype/README.md) 中的未覆盖项继续。

## 0.1.1-dev 播放修复

- 对照 Chrome 验证两部反馈影片均能播放；接入 BD1 / 4kvm、HD3 / lbyy 及同入口 bhyy。
- 支持 HLS blob 背后的实际清单，包括没有 m3u8 后缀的同站 secure.php；保持解析生命周期与媒体 URL 不落盘。
- 按设备解码能力检测不兼容格式，首帧前按同集语义换到未尝试的线路；成功出画后再提交线路和续播记录。
- 已通过 TV 模拟器两个页面入口、首帧、跳转与收藏续播；Chromecast 的前台验收仍因设备休眠待继续。
- 详细结果及 APK 校验值见 [播放修复验证](verification/playback-fix/README.md)。


## 2026-09-12：同线路软件视频解码

- [x] 接入 libVLC，通过 Media3 Player 接口复用现有 UI、MediaSession 和遥控器控制。
- [x] 原生失败先软解同一媒体，软解失败再按同集匹配其他线路；保留进度与暂停状态。
- [x] 依据真实显示帧提交播放状态与本地续播，取消过期回调和首帧等待。
- [x] 按 ABI 拆分安装包，打包 libVLC 许可文本及上游源码说明。
- [x] 构建、21 项单元测试、静态检查与模拟器 6 项用例通过；一次入口加载超时的原报告和单项复测结果均保留。
- [x] Chromecast 原 HD3 出画，确定通过一分钟吞吐检查的 ARM32 配置。
- [x] 修复布局切换造成的提前结束与误跳集，最终真机全屏/普通布局、收藏续播及首页 HLS 等 4 项回归通过；已核对电视安装包 SHA-256。

详细证据见 [软件解码验证](verification/software-decoding/README.md)。

## 2026-09-12：v0.1.2 正式发布

- [x] 独立 LIBVIO 发布签名，ARM32 / ARM64 APK 与 SHA256SUMS。
- [x] Release 构建、21 项单元测试、lint、4 项正式包 TV 模拟器播放回归。
- [x] README 下载入口与首次 Token history 基线，逐请求与截止点核验。
- [ ] 远程分支、v0.1.2 Tag、正式 Release 和公开下载校验。

见 [发布验证](verification/release-v0.1.2/README.md)。
