# v0.1.2 Token history

统计日期：2026-09-12（America/Los_Angeles）。[README](../../README.md#token-history) · [逐请求元数据和检查点](v0.1.2-2026-09-12.json)

这是 LIBVIO 首个正式发布的基线统计，覆盖本任务的调研、设计、原型、播放修复与软解真机验证；不重复计入 Olevod 既有代码的研发成本。任务内同时安装两个 app 的共用工作包含在本区间，不能可靠拆成两个独立请求成本。

区间从 **2026-09-12 10:57:17.771 PDT**（UTC 2026-09-12T17:57:17.771Z）起，到本次发布请求前 **2026-09-12 18:07:21.224 PDT**（UTC 2026-09-13T01:07:21.224Z，右端不含）。本次正式签名、Release 上传、Token history 核算和后续推送留待下一统计区间。最后一条工作用量记录为 `2026-09-13T01:00:55.096Z`。

| 阶段 | 请求数 | 输入（含缓存） | 其中缓存输入 | 输出（含推理） | 总 token | Standard API 估值 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 网站调研与设计 | 36 | 4,487,442 | 4,319,488 | 26,742 | 4,514,184 | $7.34 |
| 原型实现与设备安装 | 94 | 14,505,793 | 13,954,560 | 69,343 | 14,575,136 | $22.93 |
| HLS 与播放兼容修复 | 111 | 16,780,597 | 16,165,632 | 68,890 | 16,849,487 | $25.76 |
| 同线路软解与真机回归 | 138 | 19,115,536 | 18,741,888 | 95,622 | 19,211,158 | $27.26 |
| **合计** | **379** | **54,889,368** | **53,181,568** | **260,597** | **55,149,965** | **$83.29** |

## 核算与价格

全部工作请求使用 **GPT-6 Astra**。按 `token_usage_record.usage` 的所属任务读取，按 `response_id` 全局去重；主任务逐请求累计与冻结点的 `thread_token_usage` 完全一致。没有累加重复的界面 token_count 快照，也没有把继承上下文重复当作另一个任务的用量。

非缓存输入 **1,707,800**；推理输出 **96,076** 已包含在输出中；缓存写入记录为 0。单次最大输入 245,776，超过 272K 的请求数为 0。输入包含每次请求反复携带的上下文，不等于用户输入的字数。

2026-09-12 实际打开核对的 [GPT-6 Astra 官方价格](https://developers.openai.com/api/docs/models/gpt-6-astra)：Standard 每百万 token 非缓存输入 $10、缓存输入 $1、输出 $50、缓存写入 $12.50。超过 272K 输入的请求按完整请求输入/缓存 2 倍、输出 1.5 倍计算。

本区间公式：`(输入 - 缓存输入) × 10 / 1e6 + 缓存输入 × 1 / 1e6 + 输出 × 50 / 1e6`，未舍入合计 **$83.289418**，汇总后保留两位小数。**这是 Standard API 等价估值，不是 Codex 订阅账单、已确认服务档位或实际付款金额。**

## 单列与未统计

平台系统自动审批（`codex-auto-review`）可读取 **151 次请求、21,497,516 token**。其中一个审批任务首条记录的累计检查点额外携带 **3,249,889 token**，缺少对应逐请求明细，单列为期初检查点差额；不冒充可逐请求核验的用量。其余审批任务无差额。所有任务均验证了“可读取账本 + 单列期初差额 = 末条累计检查点”。

审批缺少可核对公开单价，费用未统计，不混入工作模型小计。图片生成、浏览器及其他工具额外费用、税费也未统计，未统计不表示零费用。

只归档时间、任务/请求标识、模型、用量和核验结果，不复制对话、凭据或原始日志。后续发布从此截止点继续增量累计，保留本次历史价格。
